## 1.1.20 — 2026-08-26 ✅ Current \/ 現行版

Google Antigravity CLI (agy) v1.1.20 対応。Termux 実機検証済み。
agy v1.1.20 support. Verified on Termux (Android ARM64).

```sh
npm install -g @bash0816/agy-termux
```

---

## 1.1.19 — 2026-08-24 ✅

Google Antigravity CLI (agy) v1.1.19 対応。Termux 実機検証済み。
agy v1.1.19 support. Verified on Termux (Android ARM64).

```sh
npm install -g @bash0816/agy-termux
```

---

## 1.1.18 — 2026-08-23 ✅

Google Antigravity CLI (agy) v1.1.18 対応。Termux 実機検証済み。
agy v1.1.18 support. Verified on Termux (Android ARM64).

```sh
npm install -g @bash0816/agy-termux
```

---

## 1.1.17 — 2026-08-22 ✅

Google Antigravity CLI (agy) v1.1.17 対応。Termux 実機検証済み。
agy v1.1.17 support. Verified on Termux (Android ARM64).

```sh
npm install -g @bash0816/agy-termux
```

---

## 1.1.14 — 2026-08-20 ✅

Google Antigravity CLI (agy) v1.1.14 対応。Termux 実機検証済み。
agy v1.1.14 support. Verified on Termux (Android ARM64).

```sh
npm install -g @bash0816/agy-termux
```

---

## 1.1.13 — 2026-08-16 ✅

upstream google-antigravity/antigravity-cli@1.1.13 追従。

```sh
npm install -g @bash0816/agy-termux
```

---

## 1.1.12 — 2026-08-14 ✅

Google Antigravity CLI (agy) v1.1.12 対応。Termux 実機検証済み。
agy v1.1.12 support. Verified on Termux (Android ARM64).

```sh
npm install -g @bash0816/agy-termux
```

---

## 1.1.10 — 2026-08-05 ✅

Google Antigravity CLI (agy) v1.1.10 対応。Termux 実機検証済み。
agy v1.1.10 support. Verified on Termux (Android ARM64).

```sh
npm install -g @bash0816/agy-termux
```

---

## 1.1.9 — 2026-08-02 ✅

Google Antigravity CLI (agy) v1.1.9 対応。Termux 実機検証済み。
agy v1.1.9 support. Verified on Termux (Android ARM64).

```sh
npm install -g @bash0816/agy-termux
```

---

## 1.1.8 — 2026-07-31 ✅

Google Antigravity CLI (agy) v1.1.8 対応。Termux 実機検証済み。
agy v1.1.8 support. Verified on Termux (Android ARM64).

```sh
npm install -g @bash0816/agy-termux
```

---

## 1.1.7 — 2026-07-27 ✅

Google Antigravity CLI (agy) v1.1.7 対応。Termux 実機検証済み。
agy v1.1.7 support. Verified on Termux (Android ARM64).

```sh
npm install -g @bash0816/agy-termux
```

---

## 1.1.6 — 2026-07-25 ✅

Google Antigravity CLI (agy) v1.1.6 対応。Termux 実機検証済み。
agy v1.1.6 support. Verified on Termux (Android ARM64).

```sh
npm install -g @bash0816/agy-termux
```

---

## 1.1.5 — 2026-07-23 ✅

Google Antigravity CLI (agy) v1.1.5 対応。Termux 実機検証済み。
agy v1.1.5 support. Verified on Termux (Android ARM64).

```sh
npm install -g @bash0816/agy-termux
```

---

## 1.1.4 — 2026-07-19 ✅

Google Antigravity CLI (agy) v1.1.4 対応。Termux 実機検証済み。
agy v1.1.4 support. Verified on Termux (Android ARM64).

```sh
npm install -g @bash0816/agy-termux
```

---

## 1.1.3 — 2026-07-17 ✅

Google Antigravity CLI (agy) v1.1.3 対応。Termux 実機検証済み。
agy v1.1.3 support. Verified on Termux (Android ARM64).

```sh
npm install -g @bash0816/agy-termux
```

---

## 1.1.2 — 2026-07-15 ✅

Google Antigravity CLI (agy) v1.1.2 対応。Termux 実機検証済み。
agy v1.1.2 support. Verified on Termux (Android ARM64).

```sh
npm install -g @bash0816/agy-termux
```

---

## 1.1.1 — 2026-07-14 ✅

Google Antigravity CLI (agy) v1.1.1 対応。Termux 実機検証済み。
agy v1.1.1 support. Verified on Termux (Android ARM64).

`agy update` コマンドのバージョン比較ロジックを堅牢化。
Improved robustness of the `agy update` version comparison logic.

```sh
npm install -g @bash0816/agy-termux
```

---

## 1.1.0 — 2026-07-09 ✅

Google Antigravity CLI (agy) v1.1.0 対応。Termux 実機検証済み。
agy v1.1.0 support. Verified on Termux (Android ARM64).

```sh
npm install -g @bash0816/agy-termux
```

---

## 1.0.16 — 2026-07-04 ✅

Google Antigravity CLI (agy) v1.0.16 対応。Termux 実機検証済み。
agy v1.0.16 support. Verified on Termux (Android ARM64).

```sh
npm install -g @bash0816/agy-termux
```

---

## 1.0.14 — 2026-07-01 ✅

upstream google-antigravity/antigravity-cli 1.0.14 追従。faccessat2 syscall による SIGSYS クラッシュを LD_PRELOAD shim で修正し、バージョンpin化により今後の破壊的変更の即時波及を防止。

```sh
npm install -g @bash0816/agy-termux
```

---

# Releases: @bash0816/agy-termux

---

## 1.0.12 — 2026-06-27 ✅

Google Antigravity CLI (agy) v1.0.12 対応。Termux 実機検証済み。

agy v1.0.12 support. Verified on Termux (Android ARM64).

- 安定性・互換性の改善 / Stability and compatibility improvements
- README / NOTICE.md 整備、npm 公開 / Added bilingual README, NOTICE.md, published to npm

```sh
npm install -g @bash0816/agy-termux
```

---

## 1.0.10-1 — 2026-06-23 (internal validation / 内部検証版)

Termux Android ARM64 互換性対応。npm 未公開の技術検証版。

Termux Android ARM64 compatibility support. Internal validation only, not published to npm.

---