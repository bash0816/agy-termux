name: npm Publish

on:
  pull_request:
    paths:
      - 'bin/**'
      - 'lib/**'
      - 'package.json'
      - 'README.md'
      - '.github/workflows/npm-publish.yml'
  push:
    branches:
      - main
    paths:
      - 'bin/**'
      - 'lib/**'
      - 'package.json'
      - 'README.md'
      - '.github/workflows/npm-publish.yml'
  workflow_dispatch:
    inputs:
      publish:
        description: 'Publish @bash0816/agy-termux to npm'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Syntax check
        run: |
          set -euo pipefail
          node --check bin/agy.js
          node --check lib/config.js
          node --check lib/downloader.js
          sh -n lib/launcher.sh

      - name: Verify config integrity
        run: |
          set -euo pipefail
          node - <<'NODE'
          const config = require('./lib/config');
          const pkg = require('./package.json');

          if (pkg.name !== '@bash0816/agy-termux') throw new Error('unexpected package name');
          if (!config.AGY_VERSION) throw new Error('missing AGY_VERSION');
          if (pkg.version !== config.AGY_VERSION) throw new Error(`package.json version (${pkg.version}) != config.AGY_VERSION (${config.AGY_VERSION})`);
          if (!/^[0-9a-f]{64}$/.test(config.SHA256_TAR)) throw new Error('invalid SHA256_TAR format');
          if (!/^[0-9a-f]{64}$/.test(config.SHA256_VA39)) throw new Error('invalid SHA256_VA39 format');
          if (!config.DOWNLOAD_URL.includes(config.AGY_VERSION)) throw new Error('DOWNLOAD_URL does not contain AGY_VERSION');
          console.log('Config OK:', config.AGY_VERSION);
          NODE

      - name: Pack dry run
        run: npm pack --dry-run

  publish:
    if: github.event_name == 'workflow_dispatch' && inputs.publish == 'true'
    needs: verify
    runs-on: ubuntu-latest
    environment: npm-publish
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Publish guard
        run: |
          set -euo pipefail
          node - <<'NODE'
          const { execSync } = require('child_process');
          const config = require('./lib/config');
          const pkg = require('./package.json');

          if (pkg.version !== config.AGY_VERSION) {
            throw new Error(`version mismatch: package.json=${pkg.version} config=${config.AGY_VERSION}`);
          }

          const packInfo = JSON.parse(execSync('npm pack --dry-run --json', { encoding: 'utf8' }));
          const pack = Array.isArray(packInfo) ? packInfo[0] : packInfo;
          const files = (pack.files || []).map(f => String(f.path || '').replace(/^\.\//, ''));
          const allowed = new Set(['package.json', 'README.md']);
          const unexpected = files.filter(f => {
            if (allowed.has(f)) return false;
            if (f.startsWith('bin/') || f.startsWith('lib/')) return false;
            return true;
          });
          if (unexpected.length) {
            throw new Error(`unexpected files in npm pack: ${unexpected.join(', ')}`);
          }
          console.log('Pack OK, files:', files);
          NODE

      - name: Publish to npm
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --access public
